def crossInfo(crossImage):
    """
    crossInfo takes the logical image of a cross and analyzes its length, width,
    angle and position in the xy-plane. 
    """
    #SizeOrgig = array med bildnes storlek
    #Resizea så att det blir en interpolering mellan värdena. Datapunkterna 
    #blir fler.
    
    return [length, width, xPos,yPos, angle]